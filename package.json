{
  "name": "accessibility",
  "version": "1.0.0",
  "description": "Accessibility module template.",
  "main": "index.html",
  "scripts": {
    "sass:watch": "sass --watch scss:css",
    "devServer": "browser-sync start --server --files 'css/*.css, *.html'",
    "start": "npm-run-all --parallel devServer sass:watch"
  },
  "author": "scraciun",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.85.1"
  }
}
